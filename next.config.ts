import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Aquí van las configuraciones de Next.js
  reactStrictMode: true,
};

// Si quieres exponer variables personalizadas
// puedes usar env o rewrites en lugar de module.exports
export default nextConfig;
